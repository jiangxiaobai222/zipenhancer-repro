# ZipEnhancer Community Vendor

This directory contains the minimum community ZipEnhancer implementation needed
to build the official-compatible backbone and load official generator weights.
Source author headers are preserved in the copied files.

Local changes are limited to packaging context; the public package imports this
vendor through `zipenhancer_repro.models.backbone`.
